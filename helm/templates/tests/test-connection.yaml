apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oci-billing-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "oci-billing-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oci-billing-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
